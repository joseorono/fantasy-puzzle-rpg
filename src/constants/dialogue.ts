/**
 * Dialogue System Constants
 */

/**
 * Maximum number of previous messages to display in the message history.
 * Keeps RAM usage low and ensures instant loading.
 * Dialogues are intentionally short by design.
 */
export const MAX_MESSAGE_HISTORY = 20;
